<%= simple_form_for @workshop do |f| %>
  <%= f.input :title %>
  <%= f.input :date %>
  <%= f.collection_select :centre_id, Centre.all, :id, :name%>
  <%= f.input :capacity %>
  <%= f.button :submit %>
<% end %>